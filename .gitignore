# --- 1. Αγνοούμε τον φάκελο που δημιουργεί το Vitis HLS ---
# Επειδή το script (run_hls.tcl) τον ξαναφτιάχνει από την αρχή,
# δεν τον θέλουμε στο GitHub. Πιάνει χώρο και έχει χιλιάδες άχρηστα αρχεία.
cong_stencil_project*/
solution1/

# --- 2. Αγνοούμε προσωρινά αρχεία Logs και Journals ---
# Αυτά τα φτιάχνει το Vivado/Vitis κάθε φορά που τρέχει
*.log
*.jou
*.str
.Xilinx/

# --- 3. Αγνοούμε τεράστια αρχεία Simulation (Waveforms) ---
# Αν κατά λάθος φτιαχτούν, είναι GBs και μπλοκάρουν το upload
*.wdb
*.vcd
*.wcfg

# --- 4. Αγνοούμε Compiled αρχεία ---
# Δεν ανεβάζουμε ποτέ .exe ή .o αρχεία
*.o
*.obj
*.exe
*.dll

# --- 5. Αγνοούμε ρυθμίσεις του VS Code ---
.vscode/